#include <stdio.h>

/**
 * main - putchar "alphabet in lowerase"
 * Return: 0
 */
int main(void)
{
	char numbers = 'a';
	for (numbers; numbers <= 'z'; char_variable++);
	putchar (numbers);
	return (0);
}
