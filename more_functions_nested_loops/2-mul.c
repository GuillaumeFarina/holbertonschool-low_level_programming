#include "main.h"

/**
 * mul - write 'function that multiplies two integers'
 * @a: checker of character a
 * @b: checker of character b
 * Return: the result
 */

int mul(int a, int b)
{
	return (a * b);
}
